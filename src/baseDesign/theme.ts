export type ThemeFeatures = {
  baseCss: boolean;
  microinteractions: boolean;
  galleryFlip: boolean;
  watchlistFlip: boolean;
  addCartFlip: boolean;
};

export type Theme = {
  features: ThemeFeatures;
};
